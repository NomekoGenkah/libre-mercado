# Prometheus — scrapea las métricas de Libre Mercado.
# app_php es alcanzable por su nombre de servicio en la red_distribuida (:80).
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: libremercado
    metrics_path: /metrics
    static_configs:
      - targets: ["app_php:80"]
